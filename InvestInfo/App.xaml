<Application x:Class="InvestInfo.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:InvestInfo">
    <Application.Resources>
        <Style TargetType="TabPanel">
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="Margin" Value="0"/>
        </Style>

        <!-- Стиль для отдельных вкладок -->
        <Style TargetType="TabItem">
            <Setter Property="Foreground" Value="#5C3D1E"/>
            <!-- Темно-коричневый текст -->
            <Setter Property="Margin" Value="-2,-4,0,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Grid>
                            <!-- Фон вкладки -->
                            <Border Name="BackgroundBorder"
                                    Background="#F5E9D8"/>
                            <!-- Светлый пергаментный фон -->

                            <!-- Цветная полоса слева (изначально прозрачная) -->
                            <Border Name="SelectionBorder"
                                    Width="4"
                                    HorizontalAlignment="Left"
                                    Background="Transparent"/>

                            <!-- Контент вкладки -->
                            <Border Name="ContentBorder"
                                    Background="Transparent"
                                    Padding="15,10">
                                <ContentPresenter x:Name="ContentSite"
                                                  VerticalAlignment="Center"
                                                  HorizontalAlignment="Left"
                                                  ContentSource="Header"
                                                  RecognizesAccessKey="True"/>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <!-- АКТИВНАЯ вкладка -->
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="BackgroundBorder" Property="Background" Value="#FAF3E8"/>
                                <!-- Светлее на 10% -->
                                <Setter TargetName="SelectionBorder" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                            <GradientStop Color="#D97B29" Offset="0"/>
                                            <!-- Оранжевый -->
                                            <GradientStop Color="#F2A742" Offset="0.5"/>
                                            <!-- Светло-оранжевый -->
                                            <GradientStop Color="#D97B29" Offset="1"/>
                                            <!-- Оранжевый -->
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="Foreground" Value="#3A250F"/>
                                <!-- Более темный коричневый -->
                            </Trigger>

                            <!-- НАВЕДЕНИЕ мыши -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="BackgroundBorder" Property="Background" Value="#F7EDDF"/>
                                <!-- Еще светлее -->
                            </Trigger>

                            <!-- НАВЕДЕНИЕ + АКТИВНАЯ (комбо) -->
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                    <Condition Property="IsSelected" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="BackgroundBorder" Property="Background" Value="#FCF8F2"/>
                                <!-- Почти белый -->
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Height" Value="45"/>
        </Style>

        <!-- Стиль для кнопок управления окном -->
        <Style TargetType="Button" x:Key="WindowButtonStyle">
            <Setter Property="Foreground" Value="#5C3D1E"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F2A742"/>
                    <!-- Оранжевый при наведении -->
                    <Setter Property="Foreground" Value="#FFFFFF"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Application.Resources>
</Application>
